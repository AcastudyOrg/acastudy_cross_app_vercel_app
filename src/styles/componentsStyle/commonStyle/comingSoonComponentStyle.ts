import { StyleSheet } from "react-native";

export const comingSoonComponentStyle = StyleSheet.create({
    imageContainer: {
      flex: 1,
      justifyContent: "center",
      alignItems: "center",
    },
    imageItem: {
      width: 400,
      height: 400,
      resizeMode: "contain",
    },
  });