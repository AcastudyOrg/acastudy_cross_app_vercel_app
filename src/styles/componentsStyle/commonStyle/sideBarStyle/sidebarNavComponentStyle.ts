import { StyleSheet } from "react-native";

export const sidebarNavComponentStyles = StyleSheet.create({
	sidebarMainContainer: {
		flex: 1,
		flexDirection: "column",
		paddingVertical: 20,
		paddingHorizontal: 10,
		backgroundColor: `rgba(0,0,0,0.2)`,
	},
	sidebarTopContainer: {
		width: "100%",
	},
});
