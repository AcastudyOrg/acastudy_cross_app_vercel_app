import { StyleSheet } from "react-native";

export const topicsSectiontyles = StyleSheet.create({
    topicsSectionContainer: {
        paddingHorizontal: "2%",
        paddingVertical: 50,
    },
    topicsSectionRow: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        marginBottom: 10,
    }
});